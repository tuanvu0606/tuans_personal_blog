<%= datagrid_form_for @grid, :method => :get, :url => specific_items_path %>

<%= paginate(@grid.assets) %>
<%= datagrid_table @grid %>
<%= paginate(@grid.assets) %>
<%= link_to 'New Specific item', new_specific_item_path%>
