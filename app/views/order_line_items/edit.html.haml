- if current_user && current_user.admin?
  %h1 Editing Order Line Item
  = render 'form', order_line_item: @order_line_item
  = link_to 'Show', @order_line_item
  |
  = link_to 'Back', order_line_items_path